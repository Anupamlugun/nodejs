const path = require("path");

const anotherpath = path.join(__dirname, "examplefolder", "example.txt");
console.log(anotherpath);

//this is a server path not the actual path it can be used further
